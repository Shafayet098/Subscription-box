import React from 'react';
import Slider from './Slider';

const SliderPreview = () => {
    return (
        <div>
            <Slider></Slider>
        </div>
    );
};

export default SliderPreview;